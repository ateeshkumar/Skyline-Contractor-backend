<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Services</title>
</head>
<body>
    <div>
        <div>
            <% if(typeof currUser == 'undefined' && !currUser) { %>
            <span><a href="/signup"><b>Sign up</b></a></span>&nbsp;&nbsp;
            <span><a href="/login"><b>Log in</b></a></span>
            <% } else { %>
            <span><a href="/logout"><b>Log out</b></a></span>
            <% } %>
        </div>

        <div>
            <a href="/listings/newRegis" class="btn btn-dark col-1 offset-3 edit-btn">Fill new opportunity</a>
        </div>
    </div>
</body>
</html>